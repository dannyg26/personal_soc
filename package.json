{
  "name": "threat-guard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/desktop-ui run dev",
    "build": "npm --prefix apps/desktop-ui run build",
    "tauri": "cargo tauri",
    "lint": "npm --prefix apps/desktop-ui run lint",
    "typecheck": "npm --prefix apps/desktop-ui run typecheck"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "dependencies": {
    "cargo": "^0.8.0"
  }
}
